extends layout

block content

    p Your email: #{user.email}
    p Your username: #{user.username}

    h1 Your posts:

    ul
        for post in posts
            li= post.content
        else
            li You have no posts yet!